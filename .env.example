# Base URL of the mlf backend API.
# Leave unset to use the built-in default (Android emulator: 10.0.2.2:3000, iOS simulator: localhost:3000).
# For a physical device, set this to your dev machine's LAN IP, e.g. http://192.168.1.20:3000
EXPO_PUBLIC_API_BASE_URL=
